<%= link_to gravatar_for(current_user, size: Settings.size_micropost), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("view_profile"), current_user %></span>
<span><%= t "micropost", count:current_user.microposts.size %></span>
